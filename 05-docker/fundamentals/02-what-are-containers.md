# What are Containers?

## Overview

This section introduces **containers**, a lightweight and portable way to package and run **applications** across different environments. It simplifies deployment by bundling applications together with their **dependencies**.

It helps build an understanding of how applications run consistently across systems, making containers essential for **Docker**, **cloud infrastructure**, **CI/CD pipelines**, and modern application development.

---

## What Are Containers?

**Containers**  
Portable and isolated environments that package an application together with its dependencies

### Role in Modern Development

- Keep applications consistent across environments  
- Package applications with required libraries and dependencies  
- Improve portability between systems and cloud platforms  
- Allow multiple applications to run in isolated environments  

Containers help developers and operations teams deploy applications more reliably and efficiently.

---

## Commands

These tools help interact with containers:

- `docker ps` → View running **containers**  
- `docker images` → View local **Docker images**  
- `docker logs` → Inspect **container logs**  
- `docker exec` → Access a running **container shell**  

---

## Key Takeaways

- **Containers** package applications with their dependencies  
- They improve portability and consistency across environments  
- Containers provide isolation between applications  
- They are lightweight compared to traditional virtual machines  
- Understanding containers is essential for working with **Docker** and modern DevOps workflows  

---

## Reflection

Learning about containers helped me understand how modern applications are packaged and deployed consistently across different systems. It made it clear why containers are widely used in development, testing, and cloud environments.

I also learned that containers simplify deployments by bundling everything an application needs into a portable unit. This reinforces the importance of understanding containerisation before working with orchestration and cloud-native technologies.
